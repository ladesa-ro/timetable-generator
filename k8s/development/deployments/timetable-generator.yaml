apiVersion: apps/v1
kind: Deployment
metadata:
  name: ladesa-ro-timetable-generator
  namespace: ladesa-ro-development
  annotations:
    secrets.infisical.com/auto-reload: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ladesa-ro-timetable-generator
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ladesa-ro-timetable-generator
    spec:
      containers:
        - name: ladesa-ro-timetable-generator
          envFrom:
           - secretRef:
               name: ladesa-ro-timetable-generator-config
               optional: false
          image: ghcr.io/ladesa-ro/timetable-generator/timetable-generator:development
          imagePullPolicy: Always
          ports:
            - name: web-port
              containerPort: 8080
          resources:
            limits:
              cpu: "1"
              memory: 1Gi
            requests:
              cpu: 50m
              memory: 100Mi
          readinessProbe:
            httpGet:
              path: /api/health/
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health/
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 15
          startupProbe:
            httpGet:
              path: /api/health/
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
      restartPolicy: Always
