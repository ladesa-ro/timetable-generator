apiVersion: v1
kind: Service
metadata:
  name: timetable-generator
  labels:
    app: timetable-generator
  namespace: ladesa
spec:
  type: NodePort
  selector:
    app: timetable-generator
  ports:
    - name: web-service
      port: 80
      targetPort: web-port